import 'package:flutter/material.dart';


class Operations extends StatelessWidget {

final int tiimeinMinute;
final int timeinSec;

  const Operations({
     Key? key, 
     required this.tiimeinMinute, 
     required this.timeinSec }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}